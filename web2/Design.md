Multi-page Web-app

Top left drop-down apps button Honba
Default App -> Screener

Apps List:
    Screener
    WorkBench
    AlgoDesigner
    Simulator
    Researcher 

This part can be inherted using ../web

App is themeable, with customizable typography and color pallet, layout presets.
layout is customizable with default-reset view. User can have their own view. Components in a window is Selectable and dockable in layout.  Default layout may be different for different apps.

All apps share data with common data layer.

The components can be reusable, other Apps also may need components such as table, chart, etc. So make it as reusable as possible.

Screener App
Markets are selectable by country, default is India, Has as search bar, and various filter knobs, heavily based on Tradingview screener UI/look-and-feel. But must use honba color pallet and typography.

Do not look into ../web directory unless instructed, there's old code that i'm not sure whether to keep it or not, so just ignore it for now.

When researching any reference app, analyze, components, placement, readability, colors, themeability, customizability, and how user friendly it is. Capture best of them and implement your version of it.